// ==========================================================
// ♾️ user.module.ts | Users Module 👥
// ==========================================================
// 🧠 Purpose:
// Handles user schema + services for profile management.
// Stub version so the app compiles without errors.
//
// 📦 Features (future):
// - User schema + service
// - Controller for CRUD
// - Role management
// ==========================================================

import { Module } from '@nestjs/common';

@Module({
  imports: [],        // will add Mongoose schema later
  controllers: [],    // will add UsersController later
  providers: [],      // will add UsersService later
  exports: [],        // will export UsersService later
})
export class UsersModule {}
