// ==========================================================
// ♾️ app.module.ts | Root Application Module 🌐
// ==========================================================
// 🧠 Purpose:
// Bootstraps the NestJS app. Right now minimal setup only.
// Future commits will add Config, Mongoose, Auth, Users, etc.
// ==========================================================

import { Module } from '@nestjs/common';

@Module({
  imports: [],
})
export class AppModule {}
