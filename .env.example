# ==========================================================
# ♾️ Avatar Engine™ | Example Environment Variables
# ==========================================================

# 🌐 Application
PORT=3000

# 🛢️ Database (MongoDB Atlas or local Mongo)
DB_USER=yourMongoUser
DB_PASSWORD=yourMongoPassword
DB_CLUSTER=yourClusterName   # e.g. cluster0.abcd123
DB_NAME=avatar_engine
APP_NAME=AvatarEngine

# Full Mongo URI is built dynamically using these values
MONGO_URI=mongodb+srv://${DB_USER}:${DB_PASSWORD}@${DB_CLUSTER}.mongodb.net/${DB_NAME}?retryWrites=true&w=majority&appName=${APP_NAME}

# 🔐 Authentication
JWT_SECRET=yourSuperSecretKeyHere
JWT_EXPIRES_IN=1d   # e.g. "1d", "12h", "60s"

# ⚙️ Environment
NODE_ENV=development
